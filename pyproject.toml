[project]
name = "emberformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "torch",
  "torchmetrics",
  "torchvision",
  "pandas",
  "rioxarray",
  "pyyaml",
  "wandb"
]
